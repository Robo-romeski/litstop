name: litstop
description: A new Flutter project.
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  google_fonts: ^6.1.0
  flutter_local_notifications: ^19.4.1
  permission_handler: ^11.3.0
  geolocator: ^10.1.0
  google_maps_flutter: ^2.10.1
  fl_chart: ^0.66.2
  sqflite: ^2.3.3+1
  shared_preferences: ^2.3.3
  provider: ^6.1.1
  intl: ^0.19.0
  firebase_core: ^3.13.0
  firebase_auth: ^5.5.3
  google_sign_in: ^6.3.0
  flutter_secure_storage: ^9.0.0
  encrypt: ^5.0.3
  local_auth: ^2.1.8
  sign_in_with_apple: ^7.0.1
  crypto: ^3.0.3
  http: ^1.2.0
  flutter_typeahead: ^5.2.0
  uuid: ^4.1.0
  url_launcher: ^6.2.2
  # flutter_keyboard_visibility: ^6.0.0  # Removed: brought transitively by flutter_typeahead >=5; avoid conflict

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  mockito: ^5.4.4
  build_runner: ^2.4.8

flutter:
  uses-material-design: true
  assets:
    - assets/images/
    - assets/icons/
